---
apiVersion: v1
kind: PersistentVolume
metadata:
  name: "pv-volume-extract"
spec:
  capacity:
    storage: 1Gi
  volumeMode: Filesystem
  accessModes:
    - ReadWriteOnce
  persistentVolumeReclaimPolicy: Delete
  storageClassName: local-storage
  local:
    path:  /run/desktop/mnt/host/c/Users/lmart/storage/extract
  nodeAffinity:
    required:
      nodeSelectorTerms:
        - matchExpressions:
            - key: kubernetes.io/hostname
              operator: In
              values:
                - lgpddev
                - docker-desktop
